ALTER TABLE ALARM_IPDR RENAME COLUMN ID to XDR_ID ;
ALTER TABLE ALARM_IPDR RENAME COLUMN VALUE to VAL ;

ALTER TABLE ARCHIVED_ALARM_IPDR RENAME COLUMN ID to XDR_ID ;
ALTER TABLE ARCHIVED_ALARM_IPDR RENAME COLUMN VALUE to VAL ;
 
CREATE OR REPLACE VIEW CLOSED_IPDR_SESSION_VIEW AS
(
   SELECT USER_NAME, SESSION_ID,DAY_OF_YEAR FROM TEMP_IPDR_0
   WHERE SESSION_STATUS = 1
 GROUP BY USER_NAME, SESSION_ID,DAY_OF_YEAR
);
