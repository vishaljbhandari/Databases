SPOOL nikira-DDL-service_number_type.log 
SET FEEDBACK OFF ;
set serveroutput off ;

CREATE TABLE SERVICE_NUMBER_TYPES
(
    ID NUMBER(20) NOT NULL,
    DESCRIPTION VARCHAR2(255) NOT NULL,
	RECORD_CONFIGS VARCHAR2(255),
	PRIMARY_AGGREGATION_TYPE NUMBER(20) NOT NULL,
    CONSTRAINT PK_SER_NUM_TYP_ID PRIMARY KEY (ID)
) ;

commit ;
